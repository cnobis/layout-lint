{
  "name": "tree-sitter-layout-lint",
  "version": "0.1.0",
  "description": "LayoutLint grammar + JS library for testing CSS layouts",
  "repository": "https://github.com/cnobis/layout-lint",
  "license": "MIT",
  "type": "module",
  "author": {
    "name": "Christopher Nobis",
    "email": "christophernobis.dev@icloud.com"
  },
  "main": "bindings/node",
  "types": "bindings/node",
  "keywords": [
    "incremental",
    "parsing",
    "tree-sitter",
    "layout_lint"
  ],
  "files": [
    "grammar.js",
    "tree-sitter.json",
    "binding.gyp",
    "prebuilds/**",
    "bindings/node/*",
    "queries/*",
    "src/**",
    "*.wasm",
    "demo/**"
  ],
  "dependencies": {
    "node-addon-api": "^8.3.1",
    "node-gyp-build": "^4.8.4",
    "web-tree-sitter": "^0.25.8"
  },
  "devDependencies": {
    "prebuildify": "^6.0.1",
    "tree-sitter-cli": "^0.25.8"
  },
  "peerDependencies": {
    "tree-sitter": "^0.22.4"
  },
  "peerDependenciesMeta": {
    "tree-sitter": {
      "optional": true
    }
  },
  "scripts": {
    "install": "node-gyp-build",
    "prestart": "tree-sitter build --wasm",
    "start": "tree-sitter playground",
    "test": "node --test bindings/node/*_test.js",
    "build:wasm": "tree-sitter build --wasm -o layout_lint.wasm",
    "serve": "npx http-server -c-1 . || npx serve -n ."
  }
}